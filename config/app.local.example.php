<?php
/* ============================================================
   CONTOH — SALIN menjadi config/app.local.php (gitignored).
   Set base URL situs publik tiket (tanpa trailing slash).
   ============================================================ */

// Produksi (admin di domain berbeda dari situs publik):
$PUBLIC_BASE_URL = 'https://ticket.vitavoxa.my.id';

// Localhost: kosongkan saja ('') agar auto-deteksi dari host.
